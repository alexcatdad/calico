{
  "name": "calico-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "lefthook install",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "bun turbo build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@changesets/cli": "^2.29.7",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "bun-types": "latest",
    "lefthook": "^2.0.4",
    "turbo": "^2.6.1",
    "typescript": "^5.0.0"
  }
}
